# Umbrella runtime requirements for the devshell.
# Service-specific images (langflow, phoenix, keybroker) pin their own deps in their Dockerfiles.
# Scenario-specific heavy deps (cyvcf2, spacy, scispaCy) live in requirements-scenarios.txt
# and are only installed when the scenarios are built — keeps the initial Codespaces
# install lean enough to fit in default storage.

# Core
pydantic>=2.0
httpx>=0.27
openai>=1.50
requests>=2.32

# Tracing
arize-phoenix-otel
openinference-instrumentation-langchain
openinference-instrumentation-openai

# CLI utilities
typer>=0.12
rich>=13
