# Umbrella runtime requirements for the devshell.
# Service-specific images (langflow, phoenix, keybroker) pin their own deps in their Dockerfiles.

# Core
pydantic>=2.0
httpx>=0.27
openai>=1.50

# Scenario A — Variant Tournament
cyvcf2>=0.30
requests>=2.32

# Scenario B — Longitudinal Ghost
spacy>=3.7
# scispacy model installed separately via pip URL in onCreateCommand.sh

# Tracing
arize-phoenix-otel
openinference-instrumentation-langchain
openinference-instrumentation-openai

# CLI utilities
typer>=0.12
rich>=13
